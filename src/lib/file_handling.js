import { v2 as cloudinary } from 'cloudinary'

cloudinary.config({
    cloud_name,
    api_key,
    api_secret
})

export const uploadThumbnail = ({thumbnail,courseId,classId}) => {
    // courses
    try {
        
    } catch (error) {
        console.log('There has been an error', error);
    }
}

export const uploadVideo = ({video,classId}) => {
    // courses
    try {
        
    } catch (error) {
        console.log('There has been an error', error);
    }
}